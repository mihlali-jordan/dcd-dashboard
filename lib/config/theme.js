import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    type: "light",
    primary: {
      main: "#442359",
    },
    secondary: {
      main: "#e64e00",
    },
    info: {
      main: "#44b4a6",
    },
  },
});

export default theme;
